# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Allow connections from any IP with password authentication (development only)
host    all             all             0.0.0.0/0               scram-sha-256
